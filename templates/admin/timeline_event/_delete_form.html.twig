<form method="post" action="{{ path('timeline_event_delete', {'id': timeline_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ timeline_event.id) }}">
    <button class="btn">Delete</button>
</form>
